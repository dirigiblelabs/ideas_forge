{
  "viewName":"IDF_IDEA_RATING",
  "query":"SELECT
	IDFI_ID,
	COUNT(DISTINCT IDFV_ID) AS TOTAL_VOTES, 
	SUM(IDFV_VOTE) AS RATING
FROM IDF_IDEA, IDF_IDEA_VOTE
WHERE IDFI_ID = IDFV_IDFI_ID
GROUP BY IDFI_ID, 
	IDFI_PUBLISH_DATE
ORDER BY RATING, TOTAL_VOTES, IDFI_PUBLISH_DATE DESC"
}
